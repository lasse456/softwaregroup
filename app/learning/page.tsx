import { DefaultHero, Underconst } from "@/components";

export default function Learning() {
  return (
    <main>
      <DefaultHero
        background="bg-[linear-gradient(rgba(0,0,0,0.45),rgba(0,0,0,0.95)),url(/hero.webp)]"
        heading="Learning"
      />
      <Underconst />
    </main>
  );
}
